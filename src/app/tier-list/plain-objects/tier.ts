/**
 * NOTES ON TIERS:
 * =========================================
 * tiers start at 1 and go as high as we want
 * (1 will be the lowest tier)
 *
 * 0 (right now) is the 'unplaced' tier
 */
export class Tier {
    name: string;
    ranking: number;
}
